---
- hosts: all
  become: yes
  gather_facts: no
  tasks:
  - name: install apache2 and httpd based on OS Platform
    apt:
      name: apache2
      state: present
      update_cache: yes
    when: ansible_facts['os_family'] == "Debian"
 # - name: install apache2 and httpd based on OS
 #   yum:
  #    name: httpd
  #    state: present
  #  when: ansible_os_family == "RedHat"   

