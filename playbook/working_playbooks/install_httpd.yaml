---
- hosts: centos
  become: yes
  tasks:
  - name: install httpd pkg
    yum:
      name: httpd
      state: present
  - name: ensure service is running
    service:
      name: httpd
      state: started

