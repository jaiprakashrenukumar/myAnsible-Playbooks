---
- hosts: all
  become: yes
  tasks:
  - name: get the ip address 
    command: "ifconfig"
  - name: get the kernal version
    command: "uname -r"
  - name: get the release details
    command: "cat /etc/issue"
